spring:
  application:
    name: ClassCheck
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/ClassCheck
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
